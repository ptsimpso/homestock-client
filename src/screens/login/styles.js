import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
  loginText: {
    textAlign: 'center',
    marginTop: 10,
  },
});
