import { StyleService } from '@ui-kitten/components';

export default StyleService.create({
  navOption: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 80,
    paddingLeft: 15,
  },
  navIcon: {
    width: 30,
    height: 30,
  },
  navText: {
    marginLeft: 15,
  },
});
