import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  form: {
    padding: 20,
  },
  input: {
    marginBottom: 10,
  },
  loading: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  submitButton: {
    height: 50,
    marginTop: 15,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
